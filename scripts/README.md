# scripts/

Operational scripts (ingest, index builds, benchmark reproduction, releases).
Each arrives with the phase that needs it; none exist yet at Phase 0.

Planned (per `docs/plan.md`):

- `ingest.py` — one-command rebuild of the document store from raw PubMed files (Phase 1).
- benchmark reproduction scripts referenced by `benchmarks/BENCHMARKS.md`.
